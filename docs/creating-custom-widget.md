- goals:
    create custom StatelessWidgets
    make widgets reuseable with constructor parsm
    style widgets with - container and BoxDecoration

- 1  before you start:
    a download game.dart in lib/
    b import it in your main.dart

- 2  anatomy of a stateless widget
    build must be included in every widget
    a build method that returns a widget
    container is for adding styling: padding marging alignment etc

- 3 using the custom widget
    just pipe it in the widgets
    its a widget eat widget eat world

- 4 using the container widget
    the conainer have params like width height and colors

- 5 box decoration
    so the containers have border align colors
    and on top of this it have a decoration params
    which an take border and color ( these cna have switches )

- 6 adding center and text to the container
    most widgets have child and text
    if you are going to make your own widgets it best be smart to follow these
    conventions

- 7 review
    build a custom stateless widget
    made widgets reuseable
    styled widgets using container and boxdecorations

